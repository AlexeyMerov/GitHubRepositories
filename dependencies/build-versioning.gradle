def versionMajor = 0
def versionMinor = 2
def versionPatch = 1
def versionFeature = 2

ext.buildVersions = [
        buildCode         : (versionMajor * 100000) + (versionMinor * 1000) + (versionPatch * 100) + versionFeature,
        buidlName         : "$versionMajor.$versionMinor.$versionPatch",
        buildFeatureSuffix: "-$versionFeature",

        minSdk            : 21,
        compileSdk        : 29,
        targetSdk         : 29,
        buildTools        : '29.0.2',

        javaCompability   : '1.8',
]